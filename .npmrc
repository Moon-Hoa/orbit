; Node 25+ ships its own native `localStorage` global (--webstorage, on by
; default), which conflicts with jsdom's localStorage shim under Vitest -
; jsdom's Storage object ends up missing its methods. Disabling Node's
; native implementation lets jsdom's own work correctly in tests.
node-options=--no-experimental-webstorage
